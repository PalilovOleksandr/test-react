export interface Article {
  objectID: string;
  title: string;
  url: string;
}
export interface OrderData {
  username: string;
  email: string;
  delivery: string;
  deliveryTime: string;
  restrictions: string[];
}
